# Grange AI — Claude Instructions

## Git workflow
- At the **start** of every session working on Grange AI: run `git pull` before touching any files.
- At the **end** of every session (after changes are made): commit and `git push`.
